'use client';

import { useEffect } from 'react';

export default function Health() {
    useEffect(() => {
        // Research how to check if the frontend is healthy
    }, []);

    return <div>Health</div>;
}
